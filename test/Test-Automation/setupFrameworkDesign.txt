

Framework.setupFramework
	@BeforeClass													141
	initializeFramework(String browser, String env)		 	   		143
		setDriver(browser, env)
			code: driver = chromeLogin(env)							43
			
			chromeLogin(String env)									61
				code: WebDriver driver = new ChromeDriver()			78
				code: driver.navigate().to(env)						80
				code: return driver									83
		(goes back to setDriver to resume execution)
	(goes back to initializeFramework to resume execution)
Execution.userAccessGroups
	@BeforeClass													28
	public void setUp() 											29
		driver=getDriver()  // in Framework.setupFramework
Framework.setupFramework
	getDriver() {													23					
		return driver												24




